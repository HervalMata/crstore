const StripePayment = (
    {
        // eslint-disable-next-line @typescript-eslint/no-unused-vars
        priceInCents,
        // eslint-disable-next-line @typescript-eslint/no-unused-vars
        orderId,
        // eslint-disable-next-line @typescript-eslint/no-unused-vars
        clientSecret,
    }: {
        priceInCents: number;
        orderId: string;
        clientSecret: string;
    }
) => {
    return <>STRIPE FORM</>
}

export default StripePayment;